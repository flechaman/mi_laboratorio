# Grafana

Configuracion para observabilidad local.

## Servicios relacionados

- Grafana: `http://localhost:3000`
- Prometheus: `http://localhost:9090`

Levantar con:

```powershell
docker compose --profile observability up -d
```
